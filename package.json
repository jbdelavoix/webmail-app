{
  "name": "webmail-app",
  "productName": "Webmail",
  "version": "1.1.0",
  "description": "Webmail — desktop client for multiple webmail accounts in one window (Electron).",
  "author": "jbdelavoix <jbdelavoix@users.noreply.github.com> (https://github.com/jbdelavoix)",
  "homepage": "https://github.com/jbdelavoix/webmail-app",
  "license": "MIT",
  "main": "src/main.js",
  "scripts": {
    "test": "node --test \"test/**/*.test.js\"",
    "vendor:sync": "node scripts/sync-vendor-assets.js",
    "dist": "npm run vendor:sync && electron-builder --config electron-builder.js --mac --linux --win --publish=never"
  },
  "devDependencies": {
    "@iconify/iconify": "^3.1.1",
    "electron": "^39.8.10",
    "electron-builder": "^26.8.1",
    "tailwindcss": "^3.4.17"
  }
}
